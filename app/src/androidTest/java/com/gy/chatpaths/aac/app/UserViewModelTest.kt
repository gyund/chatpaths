package com.gy.chatpaths.aac.app

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Test
import org.junit.runner.RunWith

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
@RunWith(AndroidJUnit4::class)
class UserViewModelTest {
    @Test
    fun userViewModelSettings() {
//        val context = ApplicationProvider.getApplicationContext<Context>()
//        val model = UserViewModel(ApplicationProvider.getApplicationContext())
//        model.userId = 1
//        assertThat(model.userId).isEqualTo(1)
//        model.userId = 2
//        assertThat(model.userId).isEqualTo(2)
//
//        val model2 = UserViewModel(ApplicationProvider.getApplicationContext())
//        assertThat(model2.userId).isEqualTo(2)
    }
}